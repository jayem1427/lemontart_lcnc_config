(author: jayem)
(date: 2026)
(Capture laser BEAM Z — prefer the Laser Setter CALIBRATE button (writes #5504).)

o<laser_calibrate> sub
    o<400> if [#<_task> EQ 1]
      (DEBUG, LASER CALIBRATE: use the tab button — jog tip into beam, then CALIBRATE)
    o<400> endif
o<laser_calibrate> endsub

M2
