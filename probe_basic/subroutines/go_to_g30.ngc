o<go_to_g30> sub
;Version date: 2026-07-09
; Tool-load / collet-change position (G53). Setter stays on #5181-#5183.
; Keep XY in sync with tool_touch_off.ngc #<tool_load_x/y_coords> (270, 100).
; Z clears first, then XY, then Z to load height.

M73 (save caller state in current call context, restore on return or endsub)
G90
G53 G0 Z0
G53 G0 X270 Y100
G53 G0 Z0

o<go_to_g30> endsub

M2
